# Screenshots

Capture these from a real run (on the fictional sample persona — safe to publish) and drop
them here, then reference them in the top-level README:

- `web-ui.png` — the single-page UI with a tailored result (fit badge, matched/missing chips,
  retrieved sources, resume + cover letter).
- `cli-run.png` — terminal output of `python -m src.cli run --source fixture --rag`
  (ingest → tailor → ranked-by-fit).
- `eval-table.png` — `python -m src.cli eval` output (RAG vs full-profile).
- `tracker.png` — the generated `tracker.md` rendered.
- `demo.gif` (optional) — a ~30–60s screen capture, or link a Loom in the README.

> Use the fictional persona for every screenshot — never your real resume or application data.
